apiVersion: v1
kind: ConfigMap
metadata:
  name: dr-barcode-service-config
  namespace: rewards-app
data:
  PORT: "4002"
  DB_HOST: "postgres-dr"
  DB_PORT: "5432"
  DB_NAME: "rewards_db_dr"
  DB_USER: "postgres"
  DB_PASSWORD: "postgres"
  NODE_ENV: "production"
  DR_MODE: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dr-barcode-service
  namespace: rewards-app
spec:
  replicas: 1  # Reduced for DR
  selector:
    matchLabels:
      app: dr-barcode-service
  template:
    metadata:
      labels:
        app: dr-barcode-service
    spec:
      containers:
      - name: dr-barcode-service
        image: dr-barcode-service:latest
        imagePullPolicy: Never  # Use local image for kind
        ports:
        - containerPort: 4002
        envFrom:
        - configMapRef:
            name: dr-barcode-service-config
        livenessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 4002
          initialDelaySeconds: 10
          periodSeconds: 5
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: dr-barcode-service
  namespace: rewards-app
spec:
  selector:
    app: dr-barcode-service
  ports:
  - port: 4002
    targetPort: 4002
  type: NodePort

